No login required. The app is a single screen: it draws the live camera image as one continuous fractal curve, thick where the image is dark and thin where it is bright. Pick a curve from the icon strip, adjust contrast, brightness or complexity with the slider, tap the shutter to hold a frame and share it. Camera permission is required; nothing is stored or uploaded. On a device without a camera the app falls back to picking a photo from the library.
